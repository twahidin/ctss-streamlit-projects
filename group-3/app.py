# -*- coding: utf-8 -*-
"""group 3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1ozS8mOZ_3b0yF91Ri5RAKm5yjxNt7wTJ
"""

# Group 3
# Members
# Leader - ziyi
# Coders - zhixiang, chazz, ashton
# Testers - idk



# zhi xiang

# chazz

# zi yi
import streamlit as st

formula = st.text_input("Enter a math equation with +,-,*,/")
formula = formula.replace(" ","")

numbers = []
operation = []
temp_number = ""

if formula:

    for ch in formula:
        if ch.isdigit() or ch == ".":
            temp_number += ch
        else:
            numbers.append(float(temp_number))
            operation.append(ch)
            temp_number = ""

    if temp_number != "":
        numbers.append(float(temp_number))

    # BODMAS
    i = 0
    while i < len(operation):
        if operation[i] == "*" or operation[i] == "/":
            if operation[i] == "*":
                result = numbers[i] * numbers[i+1]
            else:
                result = numbers[i] / numbers[i+1]

            numbers[i] = result
            numbers.pop(i+1)
            operation.pop(i)
        else:
            i += 1


    result = numbers[0]

    for i in range(len(operation)):
        if operation[i] == "+":
            result += numbers[i+1]
        else:
            result -= numbers[i+1]

    st.write("The answer is:", result)

else:
    st.write("Enter something first")

#